package com.mohit.MediCore.enums;

//    The appointmentStatus is an enum, so now we create this
public enum AppointmentStatus {

        SCHEDULED,
        CANCELLED,
        COMPLETED,
        CONFIRMED,

}
